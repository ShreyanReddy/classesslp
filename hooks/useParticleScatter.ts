// Cursor-particle interaction logic is embedded directly in ParticleField.tsx
// This hook is a stub — kept for file tree completeness per spec

export function useParticleScatter() {
  // No-op: cursor scatter physics are handled inside the Three.js animation loop
  // in components/canvas/ParticleField.tsx
}
